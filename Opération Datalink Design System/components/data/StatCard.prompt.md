Framed metric tile — oversized phosphor numeral over a dim uppercase caption.

```jsx
<StatCard value={7} label="Équipes" />
<StatCard value={23} label="Flags validés" />
```

`color` is accepted but ignored (monochrome). Lay several in a `repeat(3,1fr)` grid.
