Plain terminal panel — square frame-line border on the faint panel tint. Compose header + body. For a titled group-box, prefer `AsciiFrame`.

```jsx
<Card>
  <CardHeader label="Progression" right={<Badge variant="info">7</Badge>} />
  <CardBody>…</CardBody>
</Card>
```
