Terminal status line — the system talking back, prefixed `>>`. Self-dismisses on `[X]`.

```jsx
<Alert variant="success">Flag validé — phase_03 débloquée.</Alert>
<Alert variant="danger">Accès refusé.</Alert>
```

Variants: `success` · `danger` (blinks) · `warning` · `info`.
