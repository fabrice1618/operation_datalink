Numbered main-menu row — `[1] ▸ LABEL …… STATUS`. The hub's primary control; the whole row inverts on hover/selection. Drive it with keyboard digits or clicks.

```jsx
<MenuItem number="1" label="Briefing de mission" right="LU" onClick={…} />
<MenuItem number="3" label="Soumission des flags" right="3/5" active />
```
