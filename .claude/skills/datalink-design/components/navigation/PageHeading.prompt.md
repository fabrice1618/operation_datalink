Terminal screen title — a peak-phosphor block `▌` then the title, with an optional `// subhead` comment line. Opens every interior screen.

```jsx
<PageHeading subhead="Dossiers classifiés de l'opération">Documents d'enquête</PageHeading>
```
